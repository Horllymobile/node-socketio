{
  "name": "node-starter",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "socket.io": "^4.1.3"
  },
  "devDependencies": {
    "g": "^2.0.1",
    "nodemon": "^2.0.12"
  }
}
